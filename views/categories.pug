include header


div(class="wrap")
    div(class="content")
        header(class='size-header deep-orange darken-4')
            include site_header
        include nav
        div(class="container")
            div(class="row")
                div(class="col s12")
                    h2 #{cat[0]['category']}
                    each val, idx in goods
                        include card
                    p #{cat[0]['description']}
    include footer
