include header

div(class="wrap")
    div(class="content")
        header(class='size-header deep-orange darken-4')
            include site_header
        include nav
        div(class="container")
            div(class="row")
                div(class="col s12")
                    h2(class="title") #{goods[0].name}
                div(class="col s4")
                    img(
                        src="img/"+goods[0].image
                        class="product-image"
                    )
                div(class="col s8")
                    p #{goods[0].description}
                    span(class="success") Price: #{goods[0].cost} mdl &nbsp;&nbsp;
                    a(class="waves-effect waves-light btn add-to-cart" data-goods_id=goods[0].id) добавить в корзину
    include footer